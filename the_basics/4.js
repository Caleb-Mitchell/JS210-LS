
/*
because in this instance the `+` operator performs concatenation,
implicitly coercing `10` to `'10'` before adding the string values
every `+` operation that has a string produces a string result no matter what
the other operand is.
*/
