// ['red', 'green', 'blue', undefined]

// the function `updateColors` is defined with two parameters,
// but when it is invoked it is only passed one argument, so
// `within` the function scope references `undefined`.
