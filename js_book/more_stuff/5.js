/*

splits the string by a space, then reverses the returned array,
then transforms each element of the array based on the return value
of the callback function, in this case to the length of each string element.

*/
