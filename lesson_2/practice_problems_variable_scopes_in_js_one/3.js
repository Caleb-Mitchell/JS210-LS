// empty (at the time of line 8, baskey refers to `'empty'` from its
// declaration in the outer scope on line 1)

// play station (when `shop3` is invoked, `basket` is a new local variable,
// with function scope to `shop3`, declared on line 15).
//

// computer (as of line 23, the `basket` currently in scope is the
// one reassigned to `computer` on line 11 when `shop2` is invoked).
