/*

Exception handling allows us to handle errors in a reliable and
predictable way, preventing our programs from grinding to a halt
due to an error. We utilize this in JavaScript using the
`try`, `catch`, `throw` and `finally` keywords.

*/
