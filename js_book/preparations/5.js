// `substring` is a prototype method of the `String` constructor.
// 'create' is a static method of the 'Object' constructor.
// `fromCharCode` is a static method of the 'String' constructor.
// `slice` is a prototype method of the `String` and `Array` constructors.
// `toString` is a prototype method of all four of the `String`, `Object`, `Array`, and `Number` constructors.
