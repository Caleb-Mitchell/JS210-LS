// This will log:
// 'This is local'

// This is because this time console.log is invoked inside the local
// scope of function `someFunction`, so that is what is referenced by
// `myVar` in the local function scope.


// this is an example of variable shadowing
